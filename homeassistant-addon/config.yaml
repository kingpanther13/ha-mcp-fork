name: "Home Assistant MCP Server"
description: "AI assistant integration for Home Assistant via Model Context Protocol (MCP)"
version: "7.2.0"
slug: "ha_mcp"
url: "https://github.com/homeassistant-ai/ha-mcp"
arch:
  - aarch64 # 64-bit ARM (Raspberry Pi 4+, modern ARM devices)
  - amd64 # 64-bit x86 (most PCs and servers)
  # Note: 32-bit platforms (armhf, armv7, i386) not supported
  # uv base image only provides amd64 and arm64 builds
init: false
startup: application
boot: manual
# Enable access to Supervisor API for auto-discovery
hassio_api: true
hassio_role: default
# Enable access to Home Assistant API
homeassistant_api: true
# Enable host network mode for local network access
host_network: true
# Use pre-built Docker images
image: "ghcr.io/homeassistant-ai/ha-mcp-addon-{arch}"
# Options for user configuration
options:
  backup_hint: "normal"
  enable_skills: true
  enable_skills_as_tools: true
  enable_tool_search: false
  tool_search_max_results: 5
  enable_yaml_config_editing: false
  # --- GENERATED TOOL CONFIG START (do not edit) ---
  tools:
    add-ons:
      enabled: true
      ha_call_addon_api: "enabled-unpinned"
      ha_get_addon: "enabled-unpinned"
    areas_floors:
      enabled: true
      ha_config_list_areas: "enabled-unpinned"
      ha_config_list_floors: "enabled-unpinned"
      ha_config_remove_area: "enabled-unpinned"
      ha_config_remove_floor: "enabled-unpinned"
      ha_config_set_area: "enabled-unpinned"
      ha_config_set_floor: "enabled-unpinned"
    automations:
      enabled: true
      ha_config_get_automation: "enabled-pinned"
      ha_config_remove_automation: "enabled-unpinned"
      ha_config_set_automation: "enabled-pinned"
    blueprints:
      enabled: true
      ha_get_blueprint: "enabled-unpinned"
      ha_import_blueprint: "enabled-unpinned"
    calendar:
      enabled: true
      ha_config_get_calendar_events: "enabled-unpinned"
      ha_config_remove_calendar_event: "enabled-unpinned"
      ha_config_set_calendar_event: "enabled-unpinned"
    camera:
      enabled: true
      ha_get_camera_image: "enabled-unpinned"
    dashboards:
      enabled: true
      ha_config_delete_dashboard: "enabled-unpinned"
      ha_config_delete_dashboard_resource: "enabled-unpinned"
      ha_config_get_dashboard: "enabled-unpinned"
      ha_config_list_dashboard_resources: "enabled-unpinned"
      ha_config_set_dashboard: "enabled-unpinned"
      ha_config_set_dashboard_resource: "enabled-unpinned"
      ha_dashboard_find_card: "enabled-unpinned"
    device_registry:
      enabled: true
      ha_get_device: "enabled-unpinned"
      ha_remove_device: "enabled-unpinned"
      ha_rename_entity: "enabled-unpinned"
      ha_update_device: "enabled-unpinned"
    entity_registry:
      enabled: true
      ha_get_entity: "enabled-unpinned"
      ha_get_entity_exposure: "enabled-unpinned"
      ha_remove_entity: "enabled-unpinned"
      ha_set_entity: "enabled-unpinned"
    files:
      enabled: true
      ha_delete_file: "enabled-unpinned"
      ha_list_files: "enabled-unpinned"
      ha_read_file: "enabled-unpinned"
      ha_write_file: "enabled-unpinned"
    groups:
      enabled: true
      ha_config_list_groups: "enabled-unpinned"
      ha_config_remove_group: "enabled-unpinned"
      ha_config_set_group: "enabled-unpinned"
    hacs:
      enabled: true
      ha_hacs_add_repository: "enabled-unpinned"
      ha_hacs_download: "enabled-unpinned"
      ha_hacs_info: "enabled-unpinned"
      ha_hacs_list_installed: "enabled-unpinned"
      ha_hacs_repository_info: "enabled-unpinned"
      ha_hacs_search: "enabled-unpinned"
    helper_entities:
      enabled: true
      ha_config_list_helpers: "enabled-unpinned"
      ha_config_remove_helper: "enabled-unpinned"
      ha_config_set_helper: "enabled-unpinned"
      ha_get_helper_schema: "enabled-unpinned"
      ha_set_config_entry_helper: "enabled-unpinned"
    history_statistics:
      enabled: true
      ha_get_automation_traces: "enabled-unpinned"
      ha_get_history: "enabled-unpinned"
      ha_get_logs: "enabled-unpinned"
      ha_get_statistics: "enabled-unpinned"
    integrations:
      enabled: true
      ha_delete_config_entry: "enabled-unpinned"
      ha_get_integration: "enabled-unpinned"
      ha_set_integration_enabled: "enabled-unpinned"
    labels_categories:
      enabled: true
      ha_config_get_category: "enabled-unpinned"
      ha_config_get_label: "enabled-unpinned"
      ha_config_remove_category: "enabled-unpinned"
      ha_config_remove_label: "enabled-unpinned"
      ha_config_set_category: "enabled-unpinned"
      ha_config_set_label: "enabled-unpinned"
    scripts:
      enabled: true
      ha_config_get_script: "enabled-unpinned"
      ha_config_remove_script: "enabled-unpinned"
      ha_config_set_script: "enabled-unpinned"
    search_discovery:
      enabled: true
      ha_deep_search: "enabled-unpinned"
      ha_get_overview: "enabled-pinned"
      ha_get_state: "enabled-pinned"
      ha_get_states: "enabled-unpinned"
      ha_search_entities: "enabled-pinned"
    service_device_control:
      enabled: true
      ha_bulk_control: "enabled-unpinned"
      ha_call_service: "enabled-unpinned"
      ha_get_operation_status: "enabled-unpinned"
      ha_list_services: "enabled-unpinned"
    system:
      enabled: true
      ha_backup_create: "enabled-pinned"
      ha_backup_restore: "enabled-pinned"
      ha_check_config: "enabled-unpinned"
      ha_config_set_yaml: "enabled-pinned"
      ha_get_system_health: "enabled-unpinned"
      ha_get_updates: "enabled-unpinned"
      ha_reload_core: "enabled-pinned"
      ha_restart: "enabled-pinned"
    todo_lists:
      enabled: true
      ha_add_todo_item: "enabled-unpinned"
      ha_get_todo: "enabled-unpinned"
      ha_remove_todo_item: "enabled-unpinned"
      ha_update_todo_item: "enabled-unpinned"
    utilities:
      enabled: true
      ha_eval_template: "enabled-unpinned"
      ha_install_mcp_tools: "enabled-unpinned"
      ha_report_issue: "enabled-pinned"
    zones:
      enabled: true
      ha_get_zone: "enabled-unpinned"
      ha_remove_zone: "enabled-unpinned"
      ha_set_zone: "enabled-unpinned"
  # --- GENERATED TOOL CONFIG END ---
schema:
  backup_hint: list(strong|normal|weak|auto)
  secret_path: str?
  enable_skills: bool?
  enable_skills_as_tools: bool?
  enable_tool_search: bool?
  tool_search_max_results: int?
  enable_yaml_config_editing: bool?
  # --- GENERATED TOOL SCHEMA START (do not edit) ---
  tools:
    add-ons:
      enabled: bool?
      ha_call_addon_api: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_addon: "list(enabled-unpinned|enabled-pinned|disabled)?"
    areas_floors:
      enabled: bool?
      ha_config_list_areas: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_list_floors: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_area: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_floor: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_area: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_floor: "list(enabled-unpinned|enabled-pinned|disabled)?"
    automations:
      enabled: bool?
      ha_config_get_automation: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_automation: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_automation: "list(enabled-unpinned|enabled-pinned|disabled)?"
    blueprints:
      enabled: bool?
      ha_get_blueprint: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_import_blueprint: "list(enabled-unpinned|enabled-pinned|disabled)?"
    calendar:
      enabled: bool?
      ha_config_get_calendar_events: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_calendar_event: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_calendar_event: "list(enabled-unpinned|enabled-pinned|disabled)?"
    camera:
      enabled: bool?
      ha_get_camera_image: "list(enabled-unpinned|enabled-pinned|disabled)?"
    dashboards:
      enabled: bool?
      ha_config_delete_dashboard: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_delete_dashboard_resource: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_get_dashboard: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_list_dashboard_resources: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_dashboard: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_dashboard_resource: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_dashboard_find_card: "list(enabled-unpinned|enabled-pinned|disabled)?"
    device_registry:
      enabled: bool?
      ha_get_device: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_remove_device: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_rename_entity: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_update_device: "list(enabled-unpinned|enabled-pinned|disabled)?"
    entity_registry:
      enabled: bool?
      ha_get_entity: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_entity_exposure: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_remove_entity: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_set_entity: "list(enabled-unpinned|enabled-pinned|disabled)?"
    files:
      enabled: bool?
      ha_delete_file: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_list_files: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_read_file: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_write_file: "list(enabled-unpinned|enabled-pinned|disabled)?"
    groups:
      enabled: bool?
      ha_config_list_groups: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_group: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_group: "list(enabled-unpinned|enabled-pinned|disabled)?"
    hacs:
      enabled: bool?
      ha_hacs_add_repository: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_hacs_download: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_hacs_info: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_hacs_list_installed: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_hacs_repository_info: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_hacs_search: "list(enabled-unpinned|enabled-pinned|disabled)?"
    helper_entities:
      enabled: bool?
      ha_config_list_helpers: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_helper: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_helper: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_helper_schema: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_set_config_entry_helper: "list(enabled-unpinned|enabled-pinned|disabled)?"
    history_statistics:
      enabled: bool?
      ha_get_automation_traces: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_history: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_logs: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_statistics: "list(enabled-unpinned|enabled-pinned|disabled)?"
    integrations:
      enabled: bool?
      ha_delete_config_entry: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_integration: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_set_integration_enabled: "list(enabled-unpinned|enabled-pinned|disabled)?"
    labels_categories:
      enabled: bool?
      ha_config_get_category: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_get_label: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_category: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_label: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_category: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_label: "list(enabled-unpinned|enabled-pinned|disabled)?"
    scripts:
      enabled: bool?
      ha_config_get_script: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_remove_script: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_script: "list(enabled-unpinned|enabled-pinned|disabled)?"
    search_discovery:
      enabled: bool?
      ha_deep_search: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_overview: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_state: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_states: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_search_entities: "list(enabled-unpinned|enabled-pinned|disabled)?"
    service_device_control:
      enabled: bool?
      ha_bulk_control: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_call_service: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_operation_status: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_list_services: "list(enabled-unpinned|enabled-pinned|disabled)?"
    system:
      enabled: bool?
      ha_backup_create: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_backup_restore: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_check_config: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_config_set_yaml: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_system_health: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_updates: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_reload_core: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_restart: "list(enabled-unpinned|enabled-pinned|disabled)?"
    todo_lists:
      enabled: bool?
      ha_add_todo_item: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_get_todo: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_remove_todo_item: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_update_todo_item: "list(enabled-unpinned|enabled-pinned|disabled)?"
    utilities:
      enabled: bool?
      ha_eval_template: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_install_mcp_tools: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_report_issue: "list(enabled-unpinned|enabled-pinned|disabled)?"
    zones:
      enabled: bool?
      ha_get_zone: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_remove_zone: "list(enabled-unpinned|enabled-pinned|disabled)?"
      ha_set_zone: "list(enabled-unpinned|enabled-pinned|disabled)?"
  # --- GENERATED TOOL SCHEMA END ---
# Add-on exposes HTTP port for MCP communication (fixed internal port)
ports:
  9583/tcp: 9583
